use std::{env, process};
use rs_rust_audio::Config;
use rs_rust_audio::biquad::{AudioFilter, AudioFilterParameters};

fn main() {
    let filter = AudioFilter::new();
    let params = AudioFilterParameters::new();
    
    
}
